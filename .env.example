# Vercel KV Environment Variables
# Copy this file to .env and fill in your values
# Get these from: Vercel Dashboard → Your Project → Storage → Your KV Database → .env.local tab

KV_REST_API_URL=
KV_REST_API_TOKEN=

# Leaderboard Secret for signature verification
# Generate a random secret key for production (e.g., use: openssl rand -hex 32)
LEADERBOARD_SECRET=
